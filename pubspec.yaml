name: flutex
description: Flutter widget extensions for functional creating UI.
version: 2.0.0
homepage: https://github.com/mrxten/flutex

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.12.0"